pipeline {
    agent any

    environment {
        DOCKER_IMAGE = "quickdelivery:latest"
        HELM_RELEASE = "quickdelivery"
    }

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/Cantarella/jenkins-test.git'
            }
        }
    }
}